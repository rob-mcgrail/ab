#flashes
    - if flash[:success] || flash[:error]
        - if flash[:success]
            %p.success
                = flash[:success]
        - if flash[:error]
            %p.error
                = flash[:error]
