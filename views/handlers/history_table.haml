%thead
    %tr
        %th
            Query
        %th
            Outcome
        %th
            Opponent
        %th
            IP
%tbody
    - locals[:searches].each do |search|
        %tr
            %td
                = safe search.query_term
            %td
                = search_outcome(search, locals[:handler])
            %td
                - name = get_opponent(search, locals[:handler])
                - if name == nil
                    Unranked
                - else
                    = link_to(safe(name), "handler/#{Handler.get_id_by_name(name)}")
            %td
                = search.ip
